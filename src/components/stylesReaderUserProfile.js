import { StyleSheet } from 'react-native'

const stylesReaderUserProfile = StyleSheet.create({
    container: {
        justifyContent: 'flex-start',
        width: '100%',
        height: '100%',
        backgroundColor: '#9681DF',
        paddingTop: 40,
        display: 'flex',
    },
})

export default stylesReaderUserProfile